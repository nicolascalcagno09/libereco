import { Command } from 'simple-command-bus';

export default class GetAllContactosDestacadosCommand extends Command {

  

  // private validator: Validator;
  //
  constructor() {
    super();
  //   // this.validator = new Validator();
  //   // this.validator.validate(body, storeContactoSchema);
  //
  //   
  }
}
