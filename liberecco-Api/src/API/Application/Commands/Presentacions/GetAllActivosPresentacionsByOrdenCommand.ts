import { Command } from 'simple-command-bus';

export default class GetAllActivosPresentacionsByOrdenCommand extends Command {

  

  // private validator: Validator;
  //
  constructor() {
    super();
  //   // this.validator = new Validator();
  //   // this.validator.validate(body, storePresentacionSchema);
  //
  //   
  }
}
