import { Command } from 'simple-command-bus';

export default class GetAllRequestsCommand extends Command {

  

  // private validator: Validator;
  //
  constructor() {
    super();
  //   // this.validator = new Validator();
  //   // this.validator.validate(body, storeRequestSchema);
  //
  //   
  }
}
