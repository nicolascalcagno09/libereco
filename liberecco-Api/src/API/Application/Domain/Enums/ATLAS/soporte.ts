export enum color {
   AMARILLO,
   VERDE,
   ROJO,
   AZUL,
   NONE
}

export enum efecto {
  APAGADO,
  ENCENDIDO,
  BLINKLENTO,
  BLINKRAPIDO
}

export enum estado {
  PASO_CAJA,
  CALLE_NO_LLENA,
  CALLE_LLENA
}

export const AlarmStatus = {
  OFF : false,
  ON : true,
}